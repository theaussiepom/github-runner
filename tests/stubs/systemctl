#!/usr/bin/env bash
set -euo pipefail

calls_file="${APPLIANCE_CALLS_FILE:-/dev/null}"
printf 'systemctl %q\n' "$*" >>"$calls_file" || true

# Minimal behavior for unit tests.
if [[ "$1" == "is-active" && "$2" == "--quiet" ]]; then
	exit 1
fi

exit 0
